import './App.css';

import React, { useState } from 'react';

import Routes from './routes';

function App() {
  //const [counter, setCounter] = useState(0); // [valor, funcao  para atualizar]
  //setCounter(counter + 1);

  return (
    <div>
      <Routes></Routes>
    </div>
  );
}

export default App;
